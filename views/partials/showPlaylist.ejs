<!DOCTYPE html>
<html>
    <head>
        <%- include('header')%>
    </head>

    <body>
        <h1><%= list.name %></h1>
        <table class="table">
            <tr>
                <th>Title</th>
            </tr>
        
            <% for(let i=0; i<list.plist.length; i++){ %>
                <% let video = list.plist[i] %>
                <tr>
                    <td><%= video.title%></td>
                </tr>
                
            <% }%>
            
        </table>
        
    </body>
</html>

